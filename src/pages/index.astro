---
const language = (typeof navigator !== "undefined" && navigator.language) || "en"; // Detecta el idioma del navegador
const isSpanish = language.startsWith("es"); // Verifica si el idioma es español
const redirectUrl = isSpanish ? "/es/experience" : "/experience";
---
<head>
  <meta http-equiv="refresh" content={`0; url=${redirectUrl}`} />
</head>